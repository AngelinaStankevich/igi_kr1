def area(a):
    # Проверка на положительное значение стороны квадрата
    if a <= 0:
        return "Длина стороны должна быть положительным числом"
    return a * a

def perimeter(a):
    # Проверка на положительное значение стороны квадрата
    if a <= 0:
        return "Длина стороны должна быть положительным числом"
    return 4 * a

def display_results(a):
    # Проверка на положительное значение стороны квадрата
    if a <= 0:
        print("Ошибка: длина стороны должна быть положительным числом.")
        return
    print(f"Для квадрата со стороной {a}:")
    print(f"Площадь: {area(a)}")
    print(f"Периметр: {perimeter(a)}")

# Пример использования
side_length = float(input("Введите длину стороны квадрата: "))
display_results(side_length)
